
export default async function EliminarMascota( { id } : { id : string} ) {

    return (
        <form>
            <button>
                Eliminar mascota
            </button>
        </form>
    );
}