import { fetchMascotasPorAsociacion } from "@/lib/data";
import CartillaAnimal from "@/ui/mascotas/CartillaAnimal";
import { Mascota } from "@/lib/definiciones";

export default async function EditarMascota( { id } : { id : number} ) {


    return (
        <div>
        </div>
    );
}